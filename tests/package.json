{
  "name": "leeter",
  "version": "0.0.1",
  "description": "Simple function to convert words to leets",
  "scripts": {
    "test": "npm run test-leet && node generateReport",
    "test-leet": "./node_modules/.bin/cucumber-js -f json:reports/leet_cucumber_report.json"
  },
  "author": "Sean T",
  "license": "FREE-WARE",
  "dependencies": {
    "chai": "^4.2.0",
    "cucumber": "^5.1.0",
    "cucumber-junit-formatter": "^0.1.3",
    "cucumber-junit-reporter": "0.0.1",
    "cucumber-html-reporter": "^5.0.0"
  }
}
